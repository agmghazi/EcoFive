@model IEnumerable<Chat>


@{ ViewBag.Title = "الرئيسية";

    Layout = "_ChatRoomLayout";
}


@*<div class="chat-body">
        @foreach (var chat in Model)
        {
            <a class="room-button" asp-controller="ChatRoom" asp-action="JoinRoom" asp-route-id="@chat.Id">
                @chat.Name
            </a>
        }
    </div>*@
